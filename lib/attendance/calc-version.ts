/**
 * 근태 계산 버전.
 *
 * 마감 스냅샷은 **마감 당시의 계산 결과**를 보존한다. 계산식을 고치면 지금
 * 값이 달라지는데, 그건 근태가 바뀐 게 아니라 기준이 바뀐 것이다. 그걸
 * "마감 후 값이 바뀌었습니다"로 띄우면 마감된 모든 기간에 거짓 경보가 뜬다.
 *
 * 그래서 스냅샷에 버전을 함께 얼려두고, 버전이 다르면 값을 직접 비교하지
 * 않는다 (settle.ts 의 diffAgainstSnapshot).
 *
 * ─────────────────────────────────────────────────────────────
 * 버전을 올려야 하는 변경 — **스냅샷 6개 값 중 하나라도 달라지면**
 *
 *   targetMinutes · workedMinutes · nightMinutes
 *   holidayMinutes · overtimeMinutes · avgWeeklyMinutes
 *
 * 구체적으로는 이런 것들이다:
 *   - 실근무 산정 방식 (휴게 차감을 없애거나 구간 병합 규칙을 바꿈)
 *   - 소정근로 산정 방식의 **계산식** (설정값 변경이 아니라 코드 변경)
 *   - 법정 총량·주평균의 분모 (조직 정산기간 ↔ 개인 집계기간)
 *   - 야간·휴일 판정 구간 계산
 *   - 날짜 귀속(dayBoundaryHour) 적용 방식
 *
 * 올리지 **않는** 변경:
 *   - 화면 문구·배치·색
 *   - 조직 설정값 변경 (휴게 규칙 시간, 1일 상한 등) — 그건 "설정이 바뀌어
 *     값이 달라진 것"이고 마감 후 변경으로 드러나는 게 맞다
 *   - 스냅샷에 들어가지 않는 값 (exceedsLimitEvenIfScheduledOnly, 페이스 등)
 *   - 이름·주석·리팩터링 (값이 같으면 올리지 않는다)
 *
 * 올릴 때는 아래 표에 한 줄 남긴다. 왜 올렸는지가 없으면 다음 사람이
 * 옛 스냅샷을 어떻게 읽어야 할지 모른다.
 *
 *   1  (2026-07)  도입 시점. 구간별 추정 휴게 차감 · 개인 집계기간 분모
 * ─────────────────────────────────────────────────────────────
 */
export const CALC_VERSION = 1;
