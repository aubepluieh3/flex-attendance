node_modules/
dist/
build/
.next/
out/
.DS_Store
*.log
*.tsbuildinfo
.env
.env.*
# 필요한 환경변수 목록은 저장소에 남긴다 (값은 없음)
!.env.example
data/*.db
data/*.db-journal
.vscode/

# 확인용 임시 스크립트·스크린샷. 커밋에 섞이면 안 된다
tmp-*
tmp-shots/

# Pretendard 서브셋은 저장소에 포함한다 (self-host, CDN 의존 없음)
!public/fonts/**
