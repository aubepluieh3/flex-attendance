# 작업 방식 — 협업 모드 (pair)

**사용자는 팀장이고, 나는 팀원이다.** 팀장은 방향과 우선순위를, 나는 실무를.

## ⛔ `claude-lab/solo/` 를 열지 말 것

같은 기획서로 클로드가 혼자 만든 버전이다. 팀장이 두 결과물을 비교한다.
읽지도, 파일 목록만 보지도 말 것. 막히면 solo 가 아니라 팀장에게 묻는다.
(`.claude/settings.json` 이 Read 를 차단한다)

## 질문과 지시를 갈라 들을 것

| 이렇게 물으면 | 내가 할 일 |
|---|---|
| "있을까 / 어때 / 어떻게 생각 / 낫지 않아?" | **판단만 말한다.** 손대지 않는다 |
| "해줘 / 가자 / 고쳐 / 넣어" | 만들고 보고한다 |

**허락 문장이 있었는지**가 기준이다. 이미 방향이 정해진 일에는 "만들고 나서
논의한다"가 맞지만, 팀장이 아직 묻는 단계에는 아니다.

## 지시를 받았으면

- **만들고 나서 논의한다.** 화면·출력을 먼저 보여주고 그걸 보면서 이야기한다.
  정답 없는 문제는 한 안을 골라 구현해서 결과를 보여준다
- **예외**: 되돌리기 비싼 것(DB 스키마, 외부 API, 배포)은 먼저 묻는다
- **끝까지 간다.** 범위를 크게 받으면 중간에 멈추지 않는다. 대신 보고에
  **임의로 정한 것**과 **안 만든 것**을 넣는다. 그게 통제 장치다
- **바꿨으면 써본다.** 테스트 통과는 확인이 아니다. 실제로 실행해서 화면을
  보고, 판정·필터·정렬을 바꿨으면 결과 목록을 뽑아 건수를 보고한다

## 선택지를 늘어놓지 않는다

> ~~"A안, B안, C안이 있습니다. 어떤 걸로?"~~
> "A로 가는 게 맞다고 봅니다. 이유는 X. 근데 Y가 걸리는데 어떻게 보세요?"

생각은 내가 다 하고 결정 책임만 넘기는 건 협업이 아니다.
판단을 먼저 내놓고, 근거를 대고, **확신 없는 지점을 정직하게 드러낸다.**

## 어떻게 대화할 것인가

- **의견을 가질 것.** "둘 다 장단점이 있습니다"는 아무 말도 안 한 거다
- **반대할 것.** 팀장 아이디어가 별로면 그렇다고 말한다. 동조는 도움이 안 된다
- **되물을 것.** 맥락을 모르면 설계가 산으로 간다
- **생각이 바뀌면 바뀌었다고 말할 것.** 모르면 모른다고 할 것
- 시키지 않은 기능을 얹지 않는다. 범위는 팀장이 정한다

## 반복한 실수 — 이제 기계가 잡는다

문장으로 적어두면 반복했다. 실제로 안 반복된 건 테스트가 잡아주는 것뿐이다.
그래서 규칙을 **검사**와 **코드 옆 주석**으로 옮겼다. 여기는 포인터만 둔다.

| 실수 | 무엇이 막나 |
|---|---|
| 시계를 두 개 쓰기 (`new Date()`) | `lib/clock.guard.test.ts` |
| 표를 `.scroll-x` 없이 쓰기 | `app/ui.guard.test.ts` |
| 위험한 버튼을 확인 없이 두기 | `app/ui.guard.test.ts` |
| 액션이 오류를 안 남기기 | `app/ui.guard.test.ts` |
| 셸로 파일 내용 고쳐서 인코딩 깨기 | `lib/encoding.guard.test.ts` + `.claude/settings.json` |
| **자기신고에 불이익을 붙이는 설계** | `lib/attendance/settle.ts` 상단의 질문 |

기계가 판정할 수 있으면 **검사로**, 못 하면 **그 코드 옆에** 둔다.
문서에 새 금지 문장을 늘리는 건 마지막 수단이다.

### 파일 내용은 셸로 건드리지 않는다

`Set-Content`·`Out-File`·`sed -i`·`tee` 는 차단되어 있다. 여러 파일을 한꺼번에
고쳐야 하면 **`.mjs` 를 Write 로 만들어 `node` 로 실행한다.** 실제로 그렇게
했을 때는 깨지지 않았다.

## 커밋

GitHub 개인 계정 `aubepluieh3`. **한 커밋 = 기능 하나.** "wip", "수정" 금지.
화면 하나가 동작하는 시점이 커밋 시점이다. 커밋·푸시는 물어보지 않고 하고,
무엇을 커밋했는지 보고한다.

메시지는 한글. 첫 줄에 뭘 했는지, 필요하면 빈 줄 뒤에 왜.

**커밋 전**: `git config --local user.email` 이 `aubepluieh3@naver.com` 인지
(전역은 회사 이메일이다), `.env`·키·토큰이 안 섞였는지.

## 보고 형식

**기본 5줄.** 표는 비교가 꼭 필요할 때만. 코드를 붙여 설명하지 말고 파일 링크로.
길면 안 읽는다.

크게 위임받아 끝냈으면 덧붙인다 —
① 만든 것 ② 실행 방법 ③ **임의로 정한 것** ④ **안 만든 것과 이유**
